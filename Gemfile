# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in pagopa-wrapper-ruby.gemspec

group :development do
  gem "pry"
end

group :test do
  gem "timecop"
  gem "webmock"
end

gemspec
